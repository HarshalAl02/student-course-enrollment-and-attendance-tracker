<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>No_Duplicate_Enrollment</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
  NOT(ISBLANK(Student__c)),
  NOT(ISBLANK(Course__c)),
  ISPICKVAL(Status__c, &quot;Active&quot;),
  NOT(ISNEW()),
  Student__c = PRIORVALUE(Student__c),
  Course__c = PRIORVALUE(Course__c)
)</errorConditionFormula>
    <errorDisplayField>Student__c</errorDisplayField>
    <errorMessage>This student is already enrolled in the selected course</errorMessage>
</ValidationRule>
